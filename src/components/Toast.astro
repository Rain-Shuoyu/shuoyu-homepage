---
/* One toast for the whole document, mounted by SiteLayout. Lives at
   viewport level rather than inside any component so it can drop in
   from the top edge, centred, regardless of what triggered it. */
---

<div class="toast-region" data-toast-region role="status" aria-live="polite">
  <span class="toast glass" data-toast></span>
</div>
