---
import { site } from '../data/site';
---

<section id="now" aria-labelledby="now-heading">
  <h2 id="now-heading" class="section-kicker">NOW / 2026</h2>
  <p>{site.now}</p>
  <ul class="focus-list">
    {site.focus.map((item) => (
      <li>{item}</li>
    ))}
  </ul>
</section>
